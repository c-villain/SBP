import Foundation

public enum Constants {
    public enum BottomSheet {
        public static let cornerRadius: CGFloat = 16.0
    }
    public enum Images {
        public static let small: CGFloat = 6.0
        public static let defaults: CGFloat = 12.0
    }
    public enum Buttons {
        public static let height: CGFloat = 72.0
    }
}
